# Serving-only dependencies (M4, added 12-Aug). Not the same as the root
# requirements.txt - this is everything the API actually needs at runtime
# (pandas via features.build_features, scikit-learn/joblib to unpickle the
# model and vectorizer, fastapi/uvicorn for the API). torch, transformers,
# mlflow, dvc, and kaggle are only needed for training and would add ~2GB
# to the serving image for nothing this endpoint uses.
pandas
numpy
scikit-learn
joblib
fastapi
uvicorn
